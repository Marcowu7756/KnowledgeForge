# KnowledgeForge — 工作交接（2026-08-27）

```yaml
handoff_id: KF-HANDOFF-20260827
repo: https://github.com/Marcowu7756/KnowledgeForge
branch: main @ 1fd40b5
author_context: MarcoWu / PAILE 个人认知引擎
status: 可继续开发；working tree clean；已 push origin/main
```

---

## 1. 项目是什么

**KnowledgeForge** = 本地优先的 **PAILE**（Personal AI Learning Engine）知识编译器。

主链（冻结，勿改语义）：

```
Source → Harness → KnowledgeObject → ExpressionObject → Artifact
              ↓
    Reconstruct (Graph/Views) → Retrieve → Compose/Express
```

硬约束：

| 约束 | 含义 |
| --- | --- |
| Knowledge ≠ Expression ≠ Artifact | KO 不污染动画/音频格式 |
| Retrieve 单位 = 整卡 KO | 禁止 chunk-RAG 语义 |
| Local-first | Ollama + `models/`；cloud LLM 可选 |
| SETV 纪律 | 状态/失效/过渡/再确认；audit 未关项不宣称 FIXED |

---

## 2. 本阶段已完成（按时间）

| Commit | 内容 |
| --- | --- |
| `c1135e0` | P0–P3 主栈：KO、harness、expression、reconstruct、retrieve、compose、audio ingest |
| `dd8ca4a` | SETV 审计发布说明 |
| `8c66e80` | SETV action plan A1–A9（compose 校验、graph boost、ASR 统一、sidecar、slow tests…） |
| `41f61a2` | Windows UI v0：FastAPI 车间壳 + 五段导航 |
| `3e03853` | UI v0.2：异步 job 进度、产物预览、PyInstaller 打包脚本 |
| `1143635` | access 分级 + taxonomy 链 + ecosystem ingest + slow BGE/Whisper smoke |

**测试**：`pytest tests -q` → **70 passed**（5 个 `@pytest.mark.slow` 默认 skip）；`KF_RUN_SLOW=1` 时 **75 passed**（含 BGE + Whisper 真实冒烟）。

---

## 3. 架构层状态

| 层 | 状态 | 入口 |
| --- | --- | --- |
| P0 KO + Harness | ✅ CLOSED | `main.py compile` |
| P1 Expression | ✅ CLOSED（Manim 多 renderer DEFER） | `express` / `animate` |
| P2 Reconstruct | ✅ CLOSED | `main.py reconstruct` |
| P3 Retrieve + Compose | ✅ LANDED | `retrieve` / `compose` |
| Audio ingest | ✅ LANDED | `main.py audio` |
| Windows UI | 🟡 v0.2 ACTIVE | `main.py ui` |
| 一源多卡 / GNN / Manim 实渲染 | ⏸ HOLD | 不做 |

---

## 4. 日常命令

### 环境

```powershell
cd D:\KnowledgeForge
.\.venv\Scripts\python.exe -m pytest tests -q
```

### 主 CLI（节选）

```powershell
.\.venv\Scripts\python.exe main.py compile CARD.md --from-card --animate --fast
.\.venv\Scripts\python.exe main.py reconstruct --from-index --view theme
.\.venv\Scripts\python.exe main.py reconstruct --from-index --view contrast
.\.venv\Scripts\python.exe main.py retrieve index --from-index
.\.venv\Scripts\python.exe main.py retrieve index --from-packages
# index 后默认把 embedding 写回 data/packages/*/knowledge_object.json
# 关闭：--no-write-back-packages ；仅预览：--write-back-dry-run
.\.venv\Scripts\python.exe main.py retrieve query "美债" --top 5
.\.venv\Scripts\python.exe main.py compose lecture "主题" --top 5
.\.venv\Scripts\python.exe main.py audio path\to\file.wav
.\.venv\Scripts\python.exe main.py models status
```

### Windows UI

```powershell
.\.venv\Scripts\python.exe main.py ui
# → http://127.0.0.1:8765

.\.venv\Scripts\python.exe main.py ui --desktop   # 需 pywebview

powershell -ExecutionPolicy Bypass -File .\scripts\build_windows_ui.ps1
# → dist\KnowledgeForgeUI\KnowledgeForgeUI.exe（轻壳；重 ML 仍建议 venv + main.py ui）
```

### 慢速集成测

```powershell
$env:KF_RUN_SLOW="1"
.\.venv\Scripts\python.exe -m pytest tests/integration -q
# 或全量：pytest tests -q
```

覆盖（真实本地权重，约 1 分钟量级）：
- BGE 单句 embed + L2 归一化
- BGE 双卡索引 + retrieve 查询（`tests/integration/conftest.py` 场景卡）
- Whisper 转写 `tests/integration/fixtures/smoke_audio.wav`
- `ingest_audio` 端到端 transcript

前置：`python main.py models status` → embed + whisper 均为 ready。

### 控制台 UTF-8（中文乱码时）

```powershell
chcp 65001
$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
```

---

## 5. 目录与关键文件

```
KnowledgeForge/
├── app/
│   ├── ingest/          # 文本/音视频/YouTube/ASR
│   ├── compression/     # LLM 蒸馏
│   ├── knowledge/       # KO schema + parse
│   ├── harness/         # compile 编排
│   ├── expression/      # Visual/Audio Expression
│   ├── reconstruct/     # Graph / views / evolve
│   ├── retrieve/        # KO 嵌入索引 + graph-aware query
│   ├── compose/         # retrieve → LLM → PAPER/LECTURE
│   └── ui/              # FastAPI + static + jobs + preview + launcher
├── data/                # 运行时（多数已 gitignore）
├── docs/
│   ├── audit/           # SETV_AUDIT + ACTION_PLAN
│   └── ui/              # WINDOWS_UI_v0.md
├── tests/               # acquire / distill / locate / reorganize / integration
├── packaging/           # knowledgeforge_ui.spec
└── scripts/             # build_windows_ui.ps1
```

| 文档 | 用途 |
| --- | --- |
| `docs/audit/SETV_AUDIT_20260827.md` | 状态/失效/过渡/风险审计 |
| `docs/audit/ACTION_PLAN_20260827.md` | 先易后难修复记录 |
| `docs/ui/WINDOWS_UI_v0.md` | UI 设计与验收 |

---

## 6. UI v0.2 行为摘要

- **五段车间**：获取 → 沉淀 → 重组 → 检索 → 表达
- **长任务**：POST 返回 `job_id`，轮询 `GET /api/jobs/{id}`，前端进度条
- **预览**：`/api/preview` + 弹层；路径 sandbox 仅限 `data/`
- **打包**：`KF_ROOT` 环境变量（`app/config.py`）；frozen exe 不含 torch/whisper 等

API 要点：

| 端点 | 说明 |
| --- | --- |
| `POST /api/capture` … `compose` | 默认 `async_job: true` |
| `GET /api/jobs/{id}` | 进度与结果 |
| `GET /api/preview?path=` | MD/GIF/WAV 元数据 |
| `GET /api/preview/file?path=` | 媒体流 |

---

## 7. SETV Action Plan — 仍可选的后续

已关闭 A1–A9；**未启动或未深做**：

- ~~A8 深化：explicit `vs` 跨卡 contrast 聚类~~ ✅（`contrast_cross_ko` 边 + `--view contrast`）
- `tests/integration/fixtures/smoke_audio.wav` — Whisper 冒烟 fixture（已从 inbox 复制）
- ~~Package 级 EmbeddingRef 写回（当前 sidecar：`data/retrieve/embeddings.json`）~~ ✅ index 后默认写回 `data/packages/*/knowledge_object.json`（`--no-write-back-packages` 可关）
- 一源多卡 UI、Manim 实渲染、GNN — **仍 HOLD**

---

## 8. 外部生态：SETV / FactorLib / AShareLib

**共识（设计 + 编码）**：

这三套是个人专有知识库的**优质来源**，编译为 **分级 + 层级 taxonomy KO**，不是全量文档 RAG。

| 来源 | 适合沉淀为 KO | 不应进 KO |
| --- | --- | --- |
| SETV | 方法论、失效条件、状态转换、风险控制 | — |
| FactorLib | 因子契约、known gap、实验结论 | 原始参数、未公开策略 |
| AShareLib | A 股约束、观察合同、域边界 | 行情 raw、账户、路径 |

**Ingest CLI**（`app/ingest/ecosystem.py` + `taxonomy_registry.yaml`）：

```powershell
python main.py ecosystem ingest setv D:\fxtrading\setv\docs --dry-run
python main.py ecosystem ingest setv D:\fxtrading\setv\docs --limit 5
```

**Taxonomy 链**（纲举目张）：KO 字段 `taxonomy.path`，如 `专有知识 > SETV > 方法论 > 状态转换`；graph `--view taxonomy|contrast`；retrieve `--taxonomy-prefix`。

**Contrast 聚类（A8）**：KO 卡片 Relationship 中写 `- A vs B`（或 `对比`/`区别于` 等），reconstruct 生成 `contrast_cross_ko` 边；`--view contrast` 按 union-find 聚类展示跨卡对照组。

**受控访问（P0 — 已编码）**：

```yaml
classification: public | internal | restricted | secret
default_for_setv_factor_ashare: restricted
enforce_at:
  - ingest（打标 + 分区目录）          # ✅ default_access_for_ingest
  - retrieve / compose（filter + 禁止 secret 进 LLM prompt）  # ✅
  - git（secret/ restricted 默认不进 remote）  # ✅ data/restricted/ gitignore
  - UI export / preview                 # ✅ secret 卡 preview 拒绝
```

实现入口：`app/knowledge/access.py`；KO 字段 `access`；索引 `IndexRecord.classification`。
环境变量：`KF_ACCESS_MAX_RETRIEVE`（默认 `restricted`）、`KF_ACCESS_INCLUDE_SECRET=1` 才索引 secret。

KO 字段示例：

```yaml
access:
  classification: restricted
  source_project: factorlib
  export_policy: local_only
taxonomy:
  path: ["专有知识", "SETV", "方法论", "状态转换"]
```

已有 SETV 方法论卡示例：`data/knowledge/methodology/setv_*.md`（index 已收录；本地 data gitignore）。

---

## 9. 风险与纪律

| 风险 | 缓解 |
| --- | --- |
| 误 push secrets / venv / data | `.gitignore` 已补；提交前 `git status` |
| Cloud LLM 吃到 restricted KO | compose 前 access filter（cloud 排除 restricted/secret） |
| UI 本机泄露 | 仅 `127.0.0.1`；preview sandbox；无 auth |
| exe 不含 ML | 完整管线用 venv + `main.py ui` 或设 `KF_ROOT` |
| 宣称 graph_boost / compose 100% | 已有单测；生产仍依赖本地模型质量 |

**不要做**（除非显式解冻 HOLD）：

- Manim 实渲染管线扩展
- 一源多卡 canvas
- Chunk RAG / 聊天窗产品化
- 无分级的 SETV/Factor 全库上传 GitHub

---

## 10. 建议下一迭代（2026-08-27 更新）

| 优先级 | 项 | 状态 |
| --- | --- | --- |
| P0 | access 分级 + retrieve/compose 过滤 | ✅ 已落地 |
| P0 | ecosystem ingest + taxonomy 链 | ✅ 已落地 |
| P0 | slow smoke（BGE/Whisper） | ✅ 已落地 |
| P1 | **UI polish**：任务列表、compose 内联预览、pywebview 默认 desktop | ✅ v0.3 |
| P1 | **内容填充**：对本机 SETV/Factor/AShare 根目录跑 `ecosystem ingest` + `index rebuild` + `retrieve index` | ⬜ 待做（需本地路径） |
| P2 | A8 深化：跨卡 `vs` contrast 聚类 | ✅ `contrast_cross_ko` + `--view contrast` |
| P2 | Package 级 EmbeddingRef 写回 KO | ✅ retrieve index 默认写回 package |
| ⏸ | 一源多卡 / Manim 实渲染 / GNN / chunk-RAG 聊天 | HOLD |

---

## 11. 交接确认清单

- [x] `main` 与 `origin/main` 同步（`51be3e3` @ 2026-08-28）
- [x] 单元测试绿（92，`pytest tests -q`，见 §12）
- [x] 审计与 action plan 文档在仓
- [x] UI 可 `main.py ui` 启动
- [x] PyInstaller 脚本可产出 exe（本地已冒烟 health）
- [x] 专有知识分级与访问控制 — **已编码**（`app/knowledge/access.py` + retrieve/compose/preview/ingest）
- [x] SETV/FactorLib/AShareLib 自动 ingest 管道 — **已编码**（`ecosystem ingest` + taxonomy）
- [x] UI polish（任务列表 / compose 内联预览 / desktop 默认）— **v0.3**
- [ ] 本机专有库 bulk ingest + 索引重建 — **待操作**（非代码）

---

## 12. 单元测试跑批记录（2026-08-28）

**Commit pushed:** `51be3e3` — A8 contrast + EmbeddingRef package write-back

| 剧本 | 目录 | 结果 |
| --- | --- | --- |
| 一 获取 | `tests/acquire` | 21 passed |
| 二 沉淀 | `tests/distill` | 11 passed |
| 三 定位 | `tests/locate` | 33 passed |
| 四 重组 | `tests/reorganize` | 18 passed |
| 集成 slow | `tests/integration` | 5 passed（本机 BGE/Whisper ready） |

**合计：** 92（默认 `pytest tests -q`；含 5 slow integration）

### 本轮修复

| 问题 | 处理 |
| --- | --- |
| taxonomy 路径重复 `SETV/SETV`、`fxtrading` 段 | ✅ `taxonomy.py` 去重 + 仓库噪声过滤 |
| `reconstruct/load.py` 缺 `config` import | ✅ 已补（A8 会话） |
| integration docstring `SyntaxWarning` | ✅ 改为 raw docstring |
| package embedding 仍为 pending | ✅ retrieve index 默认写回 |

### 仍开放（未在本轮编码）

| 问题 | 说明 |
| --- | --- |
| SETV State Snapshot adapter | 设计已定；需 SETV artifact JSON 样例 |
| Semantic vs State Memory 字段 | `memory_kind` 未加 |
| FactorLib bulk ingest | 未 smoke |
| 一源多卡 / Manim / GNN | HOLD |

---

*交接人：Cursor Agent session · 2026-08-27*
